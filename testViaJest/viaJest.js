// const uppercase = (str) => {
//   return str.toUpperCase();
// };
// //don't need another return
// const lowercase = (str) => str.toLowerCase();

const uppercase = (str) => str.toUpperCase();
const lowercase = (str) => str.toLowerCase();

module.exports = {
  uppercase,
  lowercase,
};
